[project]
name = "powerpoint-to-md"
version = "1.1.0"
description = "pptx → extract.json extractor for the powerpoint-to-md skill."
requires-python = ">=3.10"
dependencies = [
    "markitdown[pptx]>=0.0.1a1",
    "python-pptx>=1.0.0",
    "pdf2image>=1.17.0",
]
